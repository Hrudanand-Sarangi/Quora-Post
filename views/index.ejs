<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Post</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Post</h1>

    <% for(post of posts){%>
        <div class="post">
            <h3 class="username">@ <%= post.username %></h3>
            <h4 class="id"><%=post.id%></h4>
            <img src="/images/img1.png" alt="post photo" id="img">
            <h4 class="content"><%= post.content %></h4>
            <button><a class="no-underline" href="http://localhost:8080/posts/<%= post.id %>">Show more</a></button>
            <button><a class="no-underline" href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a></button>
           <br>
           <br>
            <form action="/posts/<%= post.id %>?_method=DELETE" method="post">
                <button>Delete the Post</button>
            </form>
        </div>

        <% } %>
        <br>
        <br>

        <button><a class="no-underline" href="http://localhost:8080/post/new">Creat a new post</a></button>
    

    
</body>
</html>